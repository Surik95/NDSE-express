<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="<%=process.env.URL_HOME%>api/books">Главная <span class="sr-only">(current)</span></a>
            </li>
            <% if (!user) { %>
                <a class="nav-link" href="<%=process.env.URL_HOME%>api/user/login">Авторизуйтесь <span class="sr-only">(current)</span></a>
                <a class="nav-link" href="<%=process.env.URL_HOME%>api/user/signup">Зарегестрируйтесь <span class="sr-only">(current)</span></a>
            <% } else { %>
                <p><a class="nav-link" href="<%=process.env.URL_HOME%>api/user/profile"><%= user.login %><span class="sr-only">(current)</span></a></p>
            <% } %>
        </ul>
    </div>
</nav>