import Liblary from '../models/liblary.js';

export default class BooksRepository {
  //создание книги
  createBook(book) {}

  //получение книги по id
  async getBook(id) {
    await repo.getBook(id);
  }

  //получение всех книг
  getBooks() {}

  //обновление книги
  updateBook() {}

  //удаление книги
  deleteBook(id) {}
}
